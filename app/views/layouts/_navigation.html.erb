<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to "Flake's Blog", root_path, :class => "navbar-brand"%>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
      <% if user_signed_in? %>
        <li class="nav-item active">
          <%= link_to "Create blog post", posts_new_path, :class => "nav-link"%>
        </li>
      <% end %>
    </ul>
    <form class="form-inline my-2 my-lg-0">
    <% if user_signed_in? %>
      <button class="btn my-2 my-sm-0" href="#"><%= link_to 'Edit Profile', edit_user_registration_path %></button>
      <button class="btn btn-dark my-2 my-sm-0" href="#"><%= link_to 'Log out', destroy_user_session_path, method: :delete %></button>
    <% else %>
      <button class="btn my-2 my-sm-0" href="#"><%= link_to 'Sign in', sign_in_path %></button>
      <button class="btn btn-dark my-2 my-sm-0" href="#"><%= link_to 'Login', login_path %></button>
    <% end %>
    </form>
  </div>
</nav>